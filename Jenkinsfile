pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'hostname'
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls'
            }
        }
    }
}
